#include "DrawFunctions.h"

namespace term_engine::drawing {
  void DrawText(glyphs::GlyphSet& glyph_set, const glm::ivec2& start_pos, const glm::ivec2& bounds, const std::string& text, const int& text_offset = 0) {

  }
}
